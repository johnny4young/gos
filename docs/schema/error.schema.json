{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/johnny4young/gos/main/docs/schema/error.schema.json",
  "title": "gos error document",
  "description": "Printed on stdout by any command run with --json when it fails. The exit status carries the same class: 2 usage, 3 network, 4 verification, 5 lock, 1 for a generic failure.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "error": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "code",
        "message"
      ],
      "properties": {
        "code": {
          "type": "string",
          "description": "Error class; matches the exit status.",
          "enum": [
            "usage",
            "network",
            "verification",
            "lock",
            "failure"
          ]
        },
        "message": {
          "type": "string",
          "description": "Human-readable error message, the same text printed on stderr."
        }
      }
    }
  },
  "required": [
    "error"
  ]
}
